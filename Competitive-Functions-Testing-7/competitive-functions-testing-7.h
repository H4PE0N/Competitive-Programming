
/* This program contains functions that test the
functions in file competitive-functions-program-7.h*/

#ifndef COMPETITIVE_FUNCTIONS_TESTING_7_H
#define COMPETITIVE_FUNCTIONS_TESTING_7_H

// Inputs: i binary, total length, o binary;
int binary_left_shifting_test(char*, int, char*);

// Inputs: i binary, total length, o binary;
int binary_right_shifting_test(char*, int, char*);

/* Inputs: first binary, second binary, total
length, output binary; */
int binary_and_opperation_test(char*, char*, int,
  char*);

/* Inputs: first binary, second binary, total
length, output binary; */
int binary_or_opperation_test(char*, char*, int,
  char*);

/* Inputs: first binary, second binary, total
length, output binary; */
int binary_xor_opperation_test(char*, char*, int,
  char*);

/* Inputs: input binary, total length, output
binary; */
int binary_not_opperation_test(char*, char*, int,
  char*);

#endif

/* Made by Roy Hampus Fridholm */
