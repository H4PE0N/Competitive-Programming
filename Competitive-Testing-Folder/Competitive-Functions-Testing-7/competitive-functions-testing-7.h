
#ifndef COMPETITIVE_FUNCTIONS_TESTING_7_H
#define COMPETITIVE_FUNCTIONS_TESTING_7_H

// Inputs: input binary, total length, output binary;
int binary_left_shifting_test(char*, int, char*);

// Inputs: input binary, total length, output binary;
int binary_right_shifting_test(char*, int, char*);

/* Inputs: first binary, second binary, total length,
output binary; */
int binary_and_opperation_test(char*, char*, int,
  char*);

/* Inputs: first binary, second binary, total length,
output binary; */
int binary_or_opperation_test(char*, char*, int,
  char*);

/* Inputs: first binary, second binary, total length,
output binary; */
int binary_xor_opperation_test(char*, char*, int,
  char*);

// Inputs: input binary, total length, output binary;
int binary_not_opperation_test(char*, int, char*);

#endif
